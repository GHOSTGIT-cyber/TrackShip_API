TÂCHE: Ajouter flèches directionnelles sur marqueurs navires

ACTUELLEMENT:
- MapController.createShipMarker() utilise L.divIcon avec cercle coloré
- Pas d'indication de direction/cap du navire

À FAIRE:
1. Modifier createShipMarker() pour utiliser SVG avec flèche
2. Rotation basée sur ship.cog (course over ground)
3. Couleur basée sur analysis.status (alerte/vigilance/approche)

CODE SVG FLÈCHE:
```javascript
const svgIcon = `
  <svg width="24" height="24" viewBox="0 0 24 24" style="transform: rotate(${ship.cog}deg)">
    <path d="M12 2 L4 20 L12 15 L20 20 Z" fill="${color}" stroke="white" stroke-width="1"/>
  </svg>
`;
```

FICHIER: assets/js/controllers/MapController.js
MÉTHODE: createShipMarker() ligne 133
